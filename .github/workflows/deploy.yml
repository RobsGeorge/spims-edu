name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: spims-production-deploy
  # Never cancel a live production release — that can leave `artisan down` stuck.
  cancel-in-progress: false

jobs:
  test:
    uses: ./.github/workflows/ci.yml

  deploy:
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        env:
          HEALTH_URL: ${{ secrets.PRODUCTION_HEALTH_URL }}
          LOOPBACK_HEALTH_HOST: ${{ secrets.PRODUCTION_DOMAIN }}
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          timeout: 120s
          command_timeout: 30m
          envs: HEALTH_URL,LOOPBACK_HEALTH_HOST
          script: |
            set -euo pipefail
            APP_DIR="${PRODUCTION_APP_DIR:-/var/www/spims}"
            cd "$APP_DIR"
            test "$(pwd)" = "$APP_DIR"
            grep -q "APP_ENV=production" .env

            PHP_BIN=php8.2
            DEPLOY_USER="$(whoami)"
            sudo -n chown -R "${DEPLOY_USER}:www-data" storage bootstrap/cache

            git fetch --prune origin
            git reset --hard origin/main

            export PHP_BIN HEALTH_URL="${HEALTH_URL:-}" LOOPBACK_HEALTH_HOST="${LOOPBACK_HEALTH_HOST:-}"
            export QUEUE_UNIT=spims-queue
            SKIP_GIT=1 ./scripts/vps-sync.sh
            ./scripts/vps-release.sh
